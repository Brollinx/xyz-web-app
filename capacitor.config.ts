import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.calmcapybaraflip',
  appName: 'calm-capybara-flip',
  webDir: 'dist'
};

export default config;
